#include<stdio.h>
//Get inputs from the user till he enter an odd number
int main() {
    int n;
    //do{
    //    printf("Enter the number :");
    //    scanf("%d",&n);
    //}while(n%2==0);
    do{
        printf("Enter the number :")
        scanf("%d", &n);
        if (n%2==!0){
            printf("END");
            break;
        }
    }
        while(1);
    
}